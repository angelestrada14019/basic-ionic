import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_microservice',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
